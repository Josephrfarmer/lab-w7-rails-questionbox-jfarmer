<%=render 'header', post: @answer %>
<%= form_with model: @answer, local:true do |form| %>
  <%= form.label :body %>
  <%= form.text_area :body %>
  <%= form.hidden_field :user_id, :value => current_user.id %>
  <%= form.submit %>
<% end %>