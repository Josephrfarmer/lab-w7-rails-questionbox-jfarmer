<%= form_with model: @question, local:true do |form| %>
  <%= form.label :body %>
  <%= form.text_area :body %>
  <%= form.hidden_field :user_id, :value => current_user.id %>
  <%= form.submit %>
<% end %>