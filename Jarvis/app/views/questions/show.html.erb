<div class="container">
<nav class="navbar navbar-dark bg-dark">
<div class="p-2 bd-highlight"><h1 class="text-success">Jarvis</h1></div>
<%=render 'header', post: @questions %>
</nav>
<p id="notice"><%= notice %></p>

<p>
  <strong>body:</strong>
  <%= @question.body %>
</p>

<% if current_user %>
  <%= form_with(url: answers_path, local: true) do |form| %>
    <%= form.text_area :body %>
    <%= form.hidden_field :user_id, :value => current_user.id %>
    <%= form.hidden_field :question_id, :value => @question.id %>
    <%= form.submit %>
   <% end %>
<% end %>

<% @answers.each do |answer| %>
    <div>
        <div class="container-fluid"><%= answer.body %></div>
        <div class="btn-group">
        <%if (logged_in?) && (current_user.id === answer.user_id) %>
        <div><%= link_to 'Edit', edit_answer_path(answer), class:"btn btn-outline-success btn-sm btn btn-secondary" %></div>
        <%end%>
        <%if (logged_in?) && (current_user.id === answer.user_id) %>
        <%= link_to 'Delete', answer, method: :delete, data: {confirm: 'Are you sure?'}, class:"btn btn-outline-success btn-sm btn btn-secondary" %>
        <%end%>
        </div>
    </div>
  <% end %>

<%= link_to 'Back', root_path, class:"text-success" %>
</div>